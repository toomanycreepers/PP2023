package com.example.ChatModule.services;

public interface DocumentService {
    //void createDocument(DocumentDTO dto); когда разберемся с типом доков и сделаем их дто раскомментим
    void deleteDocument(long id);
    //DocumentDTO getDocument(long id); когда разберемся с типом доков и сделаем их дто раскомментим
    //DocumentDTO getDocumentsByGraduate(long graduate); когда разберемся с типом доков и сделаем их дто раскомментим
}
